'use strict'

/**
 * Returns number of items in the collection’s underlying array.
 *
 * @param {Array} array
 *
 * @returns {Number}
 */
module.exports = function size (array) {
  return array.length
}
