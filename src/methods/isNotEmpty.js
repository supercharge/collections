'use strict'

/**
 * Returns `true` when the collection is not empty,
 * `false` otherwise.
 *
 * @param {Array} array
 *
 * @returns {Boolean}
 */
module.exports = function isNotEmpty (array) {
  return array.length > 0
}
