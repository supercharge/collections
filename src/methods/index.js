'use strict'

module.exports = {
  every: require('./every'),
  filter: require('./filter'),
  find: require('./find'),
  forEach: require('./forEach'),
  map: require('./map'),
  some: require('./some')
}
