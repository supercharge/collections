'use strict'

/**
 * Returns `true` when the collection is empty,
 * `false` otherwise.
 *
 * @param {Array} array
 *
 * @returns {Boolean}
 */
module.exports = function isEmpty (array) {
  return array.length === 0
}
